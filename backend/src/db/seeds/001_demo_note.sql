-- Optional demo seed (run after 001). Uses fixed UUIDs for local QA.
-- Person 8 can align demo scenarios with these IDs.

-- Note: requires a real auth.users row if you set auth_user_id; leave null for memory-less SQL demos.
