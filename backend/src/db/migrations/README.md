# Database migrations

The canonical Supabase SQL migrations live in [`supabase/migrations/`](../../../../supabase/migrations/). Do not maintain a second copy here.

Apply them in filename order. See [database setup and learning-memory usage](../../../../docs/database.md).
