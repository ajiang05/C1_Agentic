# Frontend — copy to frontend/.env

VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Supabase Auth (browser). Optional for local demo — app works with anonymous student_id.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
